import { render } from "@testing-library/react";
import { Layout } from "../Layout"; // Replace with the correct path

describe("Layout", () => {
  it("renders without crashing", () => {
    render(<Layout />);
  });
});
